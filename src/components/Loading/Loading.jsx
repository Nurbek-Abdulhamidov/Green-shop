import React from "react";
import { LoadingSpinner, SpinnerContainer } from "./LoadingStyle";

const Loading = () => {
  return (
    <SpinnerContainer>
      <LoadingSpinner></LoadingSpinner>
    </SpinnerContainer>
  );
};

export default Loading;
